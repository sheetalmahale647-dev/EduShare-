<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Upload Notes</title>

<style>
body{
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg,#dbeafe,#eff6ff);
    margin:0;
    padding:0;
}

.container, .box{
    animation: fadeIn 0.8s ease;
}

@keyframes fadeIn{
    from{
        opacity:0;
        transform:translateY(20px);
    }
    to{
        opacity:1;
        transform:translateY(0);
    }
}

.container, .box{
    width:90%;
    max-width:500px;
    margin:40px auto;
    background:white;
    padding:30px;
    border-radius:18px;
    box-shadow:0 10px 25px rgba(0,0,0,0.12);
}

h1{
    text-align:center;
    color:#1e3a8a;
}

input, select{
    width:100%;
    padding:10px;
    margin:10px 0;
}

button{
    width:100%;
    padding:12px;
    margin-top:10px;
    border:none;
    background:linear-gradient(135deg,#2563eb,#1d4ed8);
    color:white;
    font-size:16px;
    border-radius:10px;
    cursor:pointer;
    transition:0.3s;
}

button:hover{
    transform:scale(1.03);
    box-shadow:0 8px 18px rgba(37,99,235,0.3);
}
</style>
</head>

<body>

<div class="container">

<div style="text-align:center; font-size:45px;">📚</div>
<h1>Upload Notes</h1>

<input type="text" placeholder="Enter Note Title">

<select id="class">
<option>6th</option>
<option>7th</option>
<option>8th</option>
<option>9th</option>
<option>10th</option>
</select>

<select id="subject">
<option>Computer / AI </option>
<option>Sanskrit</option>
<option>Hindi</option>
<option>Social Science</option>
<option>Maths</option>
<option>Science</option>
<option>English</option>
</select>

<input type="file">

<button onclick=üploadNote()">Upload</button>

<button onclick="location.href='dashboard.html'">Back to Dashboard</button>

</div>

<p style="text-align:center; color:gray; margin-top:20px;">
Made by NAVYA | School Project 2026
</p>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>

<script>
const supabaseUrl = "https://cexjkuowkizjzmwjqnub.supabase.co/rest/v1/L";
const supabaseKey = "sb_publishable_KJnVc7ywGt6qw5AokQ_AVA_EAX6YJZh";

const supabase = window.supabase.createClient(
    supabaseUrl,
    supabaseKey
);

console.log("Supabase Connected");
</script>
<script>
<script>
async function uploadNote(){

    const title = document.getElementById("title").value;
    const studentClass = document.getElementById("class").value;
    const subject = document.getElementById("subject").value;
    const file = document.getElementById("file").files[0];

    if(!file){
        alert("Please choose a file");
        return;
    }

    const fileName = Date.now() + "_" + file.name;

    const { data, error } = await supabase.storage
        .from("notes")
        .upload(fileName, file);

    if(error){
        alert("Upload Failed");
        console.log(error);
        return;
    }

    const fileUrl =
    supabaseUrl + "/storage/v1/object/public/notes/" + fileName;

    const { error: dbError } = await supabase
        .from("notes")
        .insert([
            {
                title: title,
                class: studentClass,
                subject: subject,
                file_url: fileUrl
            }
        ]);

    if(dbError){
        alert("Saved file but database failed");
        console.log(dbError);
        return;
    }

    alert("Notes Uploaded Successfully!");
}
</script>
</script>
</body>
</html>